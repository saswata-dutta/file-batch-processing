Take a file, split it into chunks lazily (don't load entire file into memory at once) and process them defined by the actions specified.

Processing can be concurrent, and retired based on the parameters.
